function ListTaskSpt({ listkkk }) {

    console.log(listkkk);
    return (
        <>
            {listkkk.length > 0 && listkkk.map((item => {
                return ('hol')
            }))}
        </>
    )
}

export default ListTaskSpt;
